<%- include('partials/header') %>

<div class="topbar">
  <h1>Usuarios</h1>
  <a href="/usuarios/nuevo" class="btn btn-primary"><%- icons.plus %> Nuevo usuario</a>
</div>
<p class="page-hint">Cuentas para entrar al Portal. Los usuarios con rol <strong>Cliente</strong> solo ven los datos de su propio negocio; <strong>Admin</strong> ve todos.</p>

<div class="card">
  <div class="card-header"><span><strong><%= total %></strong>&nbsp;usuario<%= total === 1 ? '' : 's' %></span></div>
  <% if (!rows.length) { %>
    <div class="empty-state">
      <%- icons.users %>
      <p>No hay usuarios cargados.</p>
    </div>
  <% } else { %>
    <div class="table-wrap">
      <table>
        <thead>
          <tr><th>Email</th><th class="col-center">Rol</th><th>Cliente</th><th class="col-center">Estado</th><th></th></tr>
        </thead>
        <tbody>
          <% rows.forEach(u => { %>
            <tr>
              <td><%= u.email %></td>
              <td class="col-center"><span class="badge badge-<%= u.rol %>"><%= u.rol %></span></td>
              <td><%= u.razonSocial || '-' %></td>
              <td class="col-center"><span class="badge <%= u.activo ? 'badge-activo' : 'badge-inactivo' %>"><%= u.activo ? 'Activo' : 'Inactivo' %></span></td>
              <td>
                <div class="cell-actions">
                  <a href="/usuarios/<%= u.id %>/editar" class="icon-btn" title="Editar"><%- icons.edit %></a>
                  <form method="POST" action="/usuarios/<%= u.id %>/borrar" class="inline"
                        onsubmit="return confirm('¿Borrar el usuario <%= u.email %>? No se puede deshacer.');">
                    <button type="submit" class="icon-btn danger" title="Borrar"><%- icons.trash %></button>
                  </form>
                </div>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
    <%- include('partials/pagination') %>
  <% } %>
</div>

<%- include('partials/footer') %>
